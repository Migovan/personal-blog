form(class="button_create" action="#!/create")
    button(type="submit") создать статью
div(class="box" ng-controller='viewCtrl' ng-repeat="blog in data track by $index")
    h2 {{blog.head}}
    //p {{blog.paper}}
    div(class="buttons")
        form(class="form_view" action="#!/view")
            button(class="button" type="submit") читать
        //form(class="form_view" action="#!/view/edit")
        //    button(class="button" type="submit") редактировать


