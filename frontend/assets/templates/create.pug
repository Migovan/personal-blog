div(ng-controller="createCtrl" class="box")
    a(ng-if="editMode" href="#!/view/{{Id}}")
        img(src="check-mark.png" ng-click="saveEditPaper()" class="image-button")
    img(ng-if="!editMode" src="add.png" ng-click="add()" class="image-button")
    form(enctype="multipart/form-data" method="post")
        h3 Добавь свою картинку!
        img(src="{{dataObject.img}}" class="image-paper")
        //img(src="{{data.img}}" class="image-paper")
        input(type="file"  ng-click="loadImage()")
    div
        h3 Как называется твоя статья?
        form
            input(ng-model="dataObject.head" class="head")
            //input(ng-model="data.head" class="head")
    div(class="window-paper")
        h3 Напишу статью здесь!
        textarea(ng-model="dataObject.paper")
        //textarea(ng-model="data.paper")




